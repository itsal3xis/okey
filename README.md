# okey
Unofficial NHL api with clear stats 

## CLI

Show basic stats for a player:

```
python okey.py -p nicksuzuki
```

Show advanced stats or bio:

```
python okey.py -p nicksuzuki -s advanced
python okey.py -p nicksuzuki -s bio
```

